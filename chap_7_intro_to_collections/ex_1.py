# If you have a list named people, how can you determine the number of entries in that list?

people = ["Zorah", "Starfire", "Storm", "Garnet"]

print(len(people)) # Using the len function can show how many elements are in a list